<% var tema = quiz.tema; %>
<form method="get" action="/quizes/<%= quiz.id %>/answer">
    <p>[Tema: <%= tema.charAt(0).toUpperCase() + tema.slice(1) %>] <%= quiz.pregunta %></p>
    <input type="text" name="respuesta" value="Respuesta"/>
    <input type="submit" value="Enviar">
</form>